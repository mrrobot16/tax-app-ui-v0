export default function CustomInstructionsForm() {
    return (
      <div className="hidden">
       {/* TODO:
        - Custom Instructions toggles form with:
                - Example custom instructions inputs
                - Text Input with label = "What would you like Copilot to know about you to provide better responses?"
                - Text Input with label = "How would you like Copilot to respond?"
                - "Enable for new chats" toggle button
                - Save button
                - Cancel button
       */}
      </div>
    );
}
