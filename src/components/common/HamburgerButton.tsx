import { HamburgerButtonProps } from 'types';

export default function HamburgerButton({ isMobile }: HamburgerButtonProps) {
    return (
      <div className="hidden">
        {/*
        TODO: For Mobile add a new chat button + hamburger button that toggles slider menu
        */}
        {/* <a onClick={()=>{}}> hamburger icon</a> */}
      </div>
    );
  }
