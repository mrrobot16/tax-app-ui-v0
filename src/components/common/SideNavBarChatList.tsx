import { UserInfo } from 'components';

export default function SideNavBarChatList() {
    return (
      <div className="hidden">
            {/*
            TODO: SideNavBar contains:
            - new chat button
            - list of chats
            */}
      </div>
    );
  }
