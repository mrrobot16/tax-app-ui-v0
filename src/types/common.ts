export type LoadingDotsProps = {
    color?: string;
    style?: string;
};

export type NewChatButtonProps = {
    isMobile: boolean
};

export type HamburgerButtonProps = {
    isMobile: boolean;
};
